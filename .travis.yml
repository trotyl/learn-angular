language: node_js
node_js:
  - "7"
before_install:
  - npm install --global yarn
install:
  - yarn global add @angular/cli
  - yarn global add ts-node
  - yarn global add typescript
  - yarn global add webpack
  - yarn install
script:
  - ts-node -P tsconfig.playbook.json playbooks/main.ts
